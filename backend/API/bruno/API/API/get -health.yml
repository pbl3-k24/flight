info:
  name: get /health
  type: http
  seq: 1
  tags:
    - API

http:
  method: GET
  url: "{{baseUrl}}/health"
  auth: inherit

runtime:
  variables:
    - name: baseUrl
      value: http://localhost:5042/index.html

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/health"
      method: GET
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
