info:
  name: get /api/v1/Reports/:reportId/download
  type: http
  seq: 3
  tags:
    - Reports

http:
  method: GET
  url: "{{baseUrl}}/api/v1/Reports/:reportId/download"
  params:
    - name: reportId
      value: ""
      type: path
  auth: inherit

runtime:
  variables:
    - name: baseUrl
      value: http://localhost:5042

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/api/v1/Reports/:reportId/download"
      method: GET
      params:
        - name: reportId
          value: ""
          type: path
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
