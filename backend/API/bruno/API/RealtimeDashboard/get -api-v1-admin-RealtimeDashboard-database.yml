info:
  name: get /api/v1/admin/RealtimeDashboard/database
  type: http
  seq: 5
  tags:
    - RealtimeDashboard

http:
  method: GET
  url: "{{baseUrl}}/api/v1/admin/RealtimeDashboard/database"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/api/v1/admin/RealtimeDashboard/database"
      method: GET
    response:
      status: 200
      statusText: OK
      headers:
        - name: Content-Type
          value: text/plain
      body:
        type: text
        data: "{}"
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/api/v1/admin/RealtimeDashboard/database"
      method: GET
    response:
      status: 200
      statusText: OK
      headers:
        - name: Content-Type
          value: application/json
      body:
        type: json
        data: "{}"
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/api/v1/admin/RealtimeDashboard/database"
      method: GET
    response:
      status: 200
      statusText: OK
      headers:
        - name: Content-Type
          value: text/json
      body:
        type: json
        data: "{}"
