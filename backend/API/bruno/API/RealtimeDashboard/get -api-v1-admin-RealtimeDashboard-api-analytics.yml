info:
  name: get /api/v1/admin/RealtimeDashboard/api-analytics
  type: http
  seq: 6
  tags:
    - RealtimeDashboard

http:
  method: GET
  url: "{{baseUrl}}/api/v1/admin/RealtimeDashboard/api-analytics"
  params:
    - name: days
      value: "30"
      type: query
  auth: inherit

runtime:
  variables:
    - name: baseUrl
      value: http://localhost:5042

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/api/v1/admin/RealtimeDashboard/api-analytics"
      method: GET
      params:
        - name: days
          value: "30"
          type: query
    response:
      status: 200
      statusText: OK
      headers:
        - name: Content-Type
          value: text/plain
      body:
        type: text
        data: "{}"
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/api/v1/admin/RealtimeDashboard/api-analytics"
      method: GET
      params:
        - name: days
          value: "30"
          type: query
    response:
      status: 200
      statusText: OK
      headers:
        - name: Content-Type
          value: application/json
      body:
        type: json
        data: "{}"
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/api/v1/admin/RealtimeDashboard/api-analytics"
      method: GET
      params:
        - name: days
          value: "30"
          type: query
    response:
      status: 200
      statusText: OK
      headers:
        - name: Content-Type
          value: text/json
      body:
        type: json
        data: "{}"
