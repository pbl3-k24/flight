info:
  name: post /api/v1/Users/forgot-password
  type: http
  seq: 5
  tags:
    - Users

http:
  method: POST
  url: "{{baseUrl}}/api/v1/Users/forgot-password"
  body:
    type: json
    data: |-
      {
        "email": ""
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/api/v1/Users/forgot-password"
      method: POST
      body:
        type: json
        data: |-
          {
            "email": ""
          }
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
