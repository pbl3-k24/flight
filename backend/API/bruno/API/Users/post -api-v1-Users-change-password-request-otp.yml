info:
  name: post /api/v1/Users/change-password/request-otp
  type: http
  seq: 7
  tags:
    - Users

http:
  method: POST
  url: "{{baseUrl}}/api/v1/Users/change-password/request-otp"
  auth: inherit

runtime:
  variables:
    - name: baseUrl
      value: http://localhost:5042

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/api/v1/Users/change-password/request-otp"
      method: POST
    response:
      status: 200
      statusText: OK
      body:
        type: json
        data: |-
          {
            "message": "OTP has been sent to your email"
          }


