# DO NOT RULES

AI MUST NOT:

* Violate architecture layers
* Generate duplicate abstractions
* Create speculative/unrequested features
* Add libraries/packages without need
* Expose EF entities directly in API
* Put business logic in controllers
* Skip validation/build steps
* Ignore compile/runtime errors
* Hardcode magic values when enum/config appropriate

